@using PagedList.Core.Mvc
@model PagedList.Core.IPagedList<WebBanSua.Models.TblTinTuc>

@{
    int CurrentPage = ViewBag.CurrentPage;
    ViewData["Title"] = "Danh sách tin đăng" + CurrentPage;
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="content-wrapper">
    <div class="page-header">
        <p class="card-description">
            <a asp-controller="Home" asp-action="Index"><code>Home</code></a><code>> Quản lý tin đăng</code><code>> Danh sách</code>
        </p>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item">
                    <a class="btn-sm btn-success " asp-action="Create">
                        <i class="fas fa-plus"></i>
                        Thêm
                    </a>
                </li>

            </ol>
        </nav>
    </div>

    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">

                    <table class="table">
                        <thead>
                            <tr>
                                <th> ID </th>
                                <th> Tiêu đề </th>

                                <th>Published </th>
                                <th> Hot news </th>
                                <th> Newfeed </th>
                                <th class="text-center">
                                    #
                                </th>

                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null)
                            {
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.PostId</td>
                                        <td>
                                            <a style="color:black;" asp-action="Details" asp-route-id="@item.PostId">@item.Title</a>

                                        </td>

                                        <td>
                                            @if (item.Published == true)
                                            {
                                                <div>
                                                    <i class="mdi mdi-toggle-switch btn-outline-success"></i> Public
                                                </div>
                                            }
                                            else
                                            {
                                                <div>
                                                    <i class="mdi mdi-toggle-switch-off btn-outline-danger"></i> Non-public
                                                </div>
                                            }
                                        </td> 
                                        <td>
                                            @if (item.IsHot == true)
                                            {
                                                <div>
                                                    <i class="mdi mdi-toggle-switch btn-outline-success"></i> Hot
                                                </div>
                                            }
                                            else
                                            {
                                                <div>
                                                    <i class="mdi mdi-toggle-switch-off btn-outline-danger"></i> -
                                                </div>
                                            }
                                        </td>
                                        <td>
                                            @if (item.IsNewfeed == true)
                                            {
                                                <div>
                                                    <i class="mdi mdi-toggle-switch btn-outline-success"></i> Newfeed
                                                </div>
                                            }
                                            else
                                            {
                                                <div>
                                                    <i class="mdi mdi-toggle-switch-off btn-outline-danger"></i> -
                                                </div>
                                            }
                                        </td>
                                        <td class="text-center">
                                            <a class="btn-sm btn-outline-warning" asp-action="Edit" asp-route-id="@item.PostId"><i class="fas fa-edit"></i></a>
                                            <a class="btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@item.PostId"><i class="fas fa-trash-alt"></i></a>
                                        </td>
                                    </tr>
                                }
                            }


                        </tbody>
                    </table>
                    <div aria-label="Page navigation example">
                        <ul class="pagination">
                            <pager class="pager-container" list="@Model" asp-area="Admin" asp-controller="AdminTblTinTucs" asp-action="Index" />
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>